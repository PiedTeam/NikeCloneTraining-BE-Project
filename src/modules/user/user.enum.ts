export enum UserVerifyStatus {
    Unverified,
    Verified,
    Banned
}

export enum UserRole {
    Admin,
    Customer
}

export enum TokenType {
    Access,
    Refresh,
    ForgotPasswordToken
}

export enum Subscription {
    False,
    True
}
