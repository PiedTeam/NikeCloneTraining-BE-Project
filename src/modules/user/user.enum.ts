export enum UserVerifyStatus {
    Unverified,
    Verified,
    Warning,
    Banned
}

export enum UserRole {
    Admin,
    Customer
}

export enum TokenType {
    Access,
    Refresh,
    ForgotPasswordToken
}

export enum Subscription {
    False,
    True
}
