export enum UserVerifyStatus {
    Unverified,
    Verified,
    Banned
}

export enum UserRole {
    Admin,
    Customer
}

export enum TokenType {
    Access,
    Refresh
}

export enum Subscription {
    False,
    True
}
